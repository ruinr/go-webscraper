version: '3'
services:

  scraperserver:
    container_name: scraperserver
    image: scraperserver:latest
    networks:
      - scrapernetwork
    links:
      - redis

  redis:
    image: redis:alpine
    networks:
      - scrapernetwork
    ports:
      - 6379:6379

networks:
  scrapernetwork:
